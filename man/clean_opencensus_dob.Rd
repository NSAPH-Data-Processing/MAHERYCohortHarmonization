% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_opencensus_dob.R
\name{clean_opencensus_dob}
\alias{clean_opencensus_dob}
\title{Clean and standardize date of birth information from OpenCensus data}
\usage{
clean_opencensus_dob(open_census_)
}
\arguments{
\item{open_census_}{A data frame containing raw date of birth fields, including \code{dob_date},
\code{dob_month}, \code{dob_year}, and \code{dob_actual}.}
}
\value{
A data frame with the following new or cleaned columns:
\describe{
\item{dob_date_clean}{A numeric day value with invalid values (e.g., 999) replaced with 1.}
\item{dob_month_clean}{A numeric month string converted from potentially French or ambiguous month names.}
\item{dob_estimated}{A logical column indicating whether the date is likely estimated.}
\item{dob_clean}{A \code{Date} object constructed from the cleaned day, month, and year values.}
}
}
\description{
This function processes date of birth fields from OpenCensus survey data, including
cleaning inconsistencies, translating French month names, handling likely placeholder
values, and producing a unified \code{Date} column for further analysis.
}
\details{
It also flags entries where the date of birth is likely to be estimated or unreliable.
\itemize{
\item French month names such as "janvier", "février", and "décembre" are translated to English and then to numeric form.
\item Dates where \code{dob_actual == "No"}, \code{dob_month == "month idr"}, or \code{dob_date} > 31 are flagged as estimated.
\item Any \code{dob_date} greater than 31 (e.g., 999 as a placeholder) is replaced with 1.
\item If \code{dob_month} is "month idr", it is assumed to be January for imputation.
}
}
