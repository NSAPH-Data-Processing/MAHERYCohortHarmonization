% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/to_pinboard.R
\name{to_pinboard}
\alias{to_pinboard}
\title{Upload output to Google Drive pin board or shared folder}
\usage{
to_pinboard(object, name, description, board, as_file = FALSE, type = "rds")
}
\arguments{
\item{object}{The R object or file path to upload.}

\item{name}{Name to save the object under (used for filename or pin name).}

\item{description}{A human-readable description (used in pin metadata).}

\item{board}{A \code{pins_board} object created by \code{board_gdrive()}.}

\item{as_file}{If \code{TRUE}, upload directly to Drive; otherwise use \code{pins}.}

\item{type}{File type (e.g. "csv", "rds", "png"). Only needed for \code{pins}.}
}
\value{
A message indicating success and upload details.
}
\description{
This function standardizes how we publish outputs from the pipeline
to a shared Google Drive folder. If the object is text-compatible
(data frames, CSVs, R objects), it uses \code{pins::pin_write()} for
versioning. If it's a binary file (e.g., image, PDF), it uses
\code{googledrive::drive_upload()} instead.
}
\examples{
# Example usage of to_pinboard function
to_pinboard(
  object = mtcars,
  name = "mtcars",
  description = "A dataset of motor trend car road tests",
  board = mahery_board,
  as_file = FALSE,
  type = "csv"
)
}
